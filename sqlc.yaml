version: "2"
sql:
  - schema: "pkg/event_store/postgres/db/migration"
    queries: "pkg/event_store/postgres/db/query"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "pkg/event_store/postgres/db"
        emit_interface: true
        emit_json_tags: true

  - schema: "internal/db/migration"
    queries: "internal/db/query"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_interface: true
        emit_json_tags: true
